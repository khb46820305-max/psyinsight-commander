# PsyInsight Commander 배포 가이드

## 🌐 외부 접속 가능한 웹사이트로 만들기

로컬호스트가 아닌 인터넷 어디서나 접속 가능한 웹사이트로 배포하는 방법입니다.

---

## 🚀 방법 1: Streamlit Cloud (추천 - 가장 쉬움, 무료)

### 장점
- ✅ 완전 무료
- ✅ 설정이 매우 간단
- ✅ GitHub 연동으로 자동 배포
- ✅ HTTPS 자동 지원
- ✅ 커스텀 도메인 지원 (선택)

### 단계별 설정

#### 1단계: GitHub에 코드 업로드

1. GitHub 계정 생성 (없는 경우)
   - https://github.com/ 접속

2. 새 저장소 생성
   - "New repository" 클릭
   - 저장소 이름: `psyinsight-commander`
   - Public 또는 Private 선택
   - "Create repository" 클릭

3. 코드 업로드
   ```bash
   # Git 초기화 (아직 안 했다면)
   git init
   git add .
   git commit -m "Initial commit"
   
   # GitHub 저장소 연결
   git remote add origin https://github.com/당신의계정/psyinsight-commander.git
   git branch -M main
   git push -u origin main
   ```

#### 2단계: Streamlit Cloud에 배포

1. Streamlit Cloud 접속
   - https://streamlit.io/cloud 접속
   - "Sign up" 또는 "Log in" 클릭
   - GitHub 계정으로 로그인

2. 앱 배포
   - "New app" 클릭
   - 저장소 선택: `psyinsight-commander`
   - Main file path: `app.py`
   - "Deploy!" 클릭

3. Secrets 설정 (환경 변수)
   - 앱 설정에서 "Secrets" 클릭
   - 다음 내용 입력:
   ```toml
   GEMINI_API_KEY = "AIzaSyDvgTdju4cNj-x4MNXV_8Hv2xRRrS8rZ-o"
   EMAIL_SENDER = "khb46820305@gmail.com"
   EMAIL_PASSWORD = "*apel*0305"
   EMAIL_RECEIVER = "khb4682@naver.com"
   ```

4. 완료!
   - 배포 완료 후 `https://당신의계정-psyinsight-commander.streamlit.app` 형태의 URL이 생성됩니다
   - 이 URL을 누구에게나 공유할 수 있습니다!

---

## 🚀 방법 2: Railway (무료 티어, 더 많은 제어)

### 장점
- ✅ 무료 티어 제공 ($5 크레딧/월)
- ✅ 자동 배포
- ✅ 데이터베이스 호스팅 가능

### 설정 방법

1. Railway 접속: https://railway.app/
2. GitHub로 로그인
3. "New Project" → "Deploy from GitHub repo"
4. 저장소 선택
5. 환경 변수 설정 (Secrets)
6. 배포 완료!

---

## 🚀 방법 3: Render (무료 티어)

### 장점
- ✅ 무료 티어 제공
- ✅ 자동 배포

### 설정 방법

1. Render 접속: https://render.com/
2. "New Web Service" 선택
3. GitHub 저장소 연결
4. 설정:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `streamlit run app.py --server.port $PORT --server.address 0.0.0.0`
5. 환경 변수 설정
6. 배포!

---

## ⚠️ 주의사항

### 1. API 키 보안
- ✅ `.env` 파일은 절대 GitHub에 업로드하지 마세요
- ✅ `.gitignore`에 `.env`가 포함되어 있는지 확인
- ✅ Streamlit Cloud Secrets에만 API 키 입력

### 2. 데이터베이스
- 현재 SQLite는 로컬 파일이므로 배포 시 문제가 될 수 있습니다
- 해결책:
  - Streamlit Cloud: 세션마다 새로 생성됨 (임시)
  - Railway/Render: PostgreSQL 사용 권장

### 3. 자동화 스크립트
- `collect_news.py`, `collect_papers.py`는 클라우드에서 실행하려면:
  - GitHub Actions 사용
  - 또는 Railway/Render의 Cron Job 사용

---

## 🔧 배포 전 체크리스트

- [ ] `.env` 파일이 `.gitignore`에 포함되어 있는지 확인
- [ ] `requirements.txt`가 최신인지 확인
- [ ] GitHub에 코드 업로드 완료
- [ ] Streamlit Cloud/Railway/Render에 Secrets 설정 완료
- [ ] 배포 후 테스트

---

## 📝 Streamlit Cloud 배포용 설정 파일

`.streamlit/config.toml` 파일 생성 (선택 사항):

```toml
[server]
headless = true
port = 8501

[browser]
gatherUsageStats = false
```

---

## 🎯 추천: Streamlit Cloud

**가장 간단하고 빠른 방법:**
1. GitHub에 코드 업로드 (5분)
2. Streamlit Cloud에서 배포 (2분)
3. Secrets 설정 (1분)
4. 완료! (총 8분)

**결과:**
- 인터넷 어디서나 접속 가능한 웹사이트
- HTTPS 자동 지원
- 완전 무료
- 자동 업데이트 (GitHub 푸시 시)

---

**지금 바로 배포하시겠어요? GitHub 저장소 생성부터 도와드릴 수 있습니다!**
