# 배포 가이드 (통합)

이 문서는 다음 파일들을 통합했습니다:
- 배포가이드.md
- 배포준비완료.md
- 배포진행중.md
- 배포완료보고.md
- StreamlitCloud배포가이드.md
- GitHub저장소생성가이드.md
- GitHub인증가이드.md
- GitHub푸시방법.md
- 빠른푸시가이드.md

---

## Streamlit Cloud 배포 방법

### 1. GitHub 저장소 생성
1. GitHub에 로그인
2. 새 저장소 생성 (예: `psyinsight-commander`)
3. 저장소 URL 복사

### 2. GitHub에 코드 푸시
```bash
# 저장소 연결
git remote add origin https://github.com/사용자명/psyinsight-commander.git

# Personal Access Token 생성 (Settings > Developer settings > Personal access tokens)
# 권한: repo (전체)

# 푸시
git push origin main
```

### 3. Streamlit Cloud 배포
1. https://streamlit.io/cloud 접속
2. GitHub 계정으로 로그인
3. "New app" 클릭
4. 저장소 선택
5. Main file path: `app.py`
6. Secrets에 API 키 추가:
   ```
   GEMINI_API_KEY=your_api_key
   EMAIL_SENDER=your-email@gmail.com
   EMAIL_PASSWORD=your_app_password
   EMAIL_RECEIVER=receiver@example.com
   ```
7. "Deploy" 클릭

### 4. 배포 완료
- 배포 후 자동으로 URL 생성
- 코드 변경 시 자동 재배포

---

## GitHub 인증 방법

### Personal Access Token 생성
1. GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
2. "Generate new token" 클릭
3. 권한 선택: `repo` (전체)
4. 토큰 생성 후 복사 (한 번만 표시됨)

### 토큰 사용
```bash
git remote set-url origin https://토큰@github.com/사용자명/저장소명.git
git push origin main
```

---

## 빠른 푸시 가이드

```bash
# 1. 변경사항 추가
git add .

# 2. 커밋
git commit -m "변경사항 설명"

# 3. 푸시
git push origin main
```

---

## 주의사항

- API 키는 절대 코드에 직접 작성하지 마세요
- Streamlit Cloud Secrets에만 저장하세요
- 토큰은 안전하게 보관하세요
