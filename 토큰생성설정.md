# Personal Access Token 생성 설정

## 📝 입력 내용

### 1. Token name (토큰 이름)
```
PsyInsight Commander
```
또는
```
psyinsight-commander
```

### 2. Description (설명) - 선택사항
```
For PsyInsight Commander project deployment
```

### 3. Expiration (만료일)
- **30 days** (기본값) 또는
- **90 days** (더 길게 사용하려면)
- 또는 **Custom** (원하는 날짜 선택)

### 4. Repository access (저장소 접근)
**"Only select repositories"** 선택
- 그 다음 `psyinsight-commander` 저장소 체크

또는

**"All repositories"** 선택 (더 간단)

### 5. Permissions (권한)
**Repository permissions** 섹션에서:

✅ **Contents**: `Read and write` 선택
- 코드 푸시/풀에 필요

✅ **Metadata**: `Read-only` (자동으로 체크됨)
- 저장소 정보 읽기

**다른 권한은 필요 없습니다!**

### 6. Generate token 클릭
- 생성된 토큰을 **반드시 복사**하세요!
- ⚠️ 한 번만 보이므로 잃어버리면 다시 생성해야 합니다

---

## ✅ 체크리스트

- [ ] Token name: `PsyInsight Commander`
- [ ] Expiration: `30 days` 또는 `90 days`
- [ ] Repository access: `Only select repositories` → `psyinsight-commander` 선택
- [ ] Permissions: `Contents` → `Read and write`
- [ ] Generate token 클릭
- [ ] 토큰 복사 (중요!)

---

## 🔐 토큰 생성 후

토큰을 복사하셨으면:
1. 안전한 곳에 보관
2. 제가 푸시할 때 사용
3. 또는 직접 푸시 시도
